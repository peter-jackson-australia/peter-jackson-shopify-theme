<style>
  .main-blog-collection {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding-bottom: var(--space-2xl);
  }

  .main-blog-collection__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-2xs);
  }

  .main-blog-collection__post {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    overflow: hidden;
  }

  .main-blog-collection__post-image {
    display: block;
    width: 100%;
    overflow: hidden;
    margin-bottom: var(--space-xs);
  }

  .main-blog-collection__post-image img {
    width: 100%;
    object-fit: cover;
  }

  .main-blog-collection__post-content {
    padding-left: var(--space-m);
  }

  .main-blog-collection__post-category {
    color: var(--neutral-600);
    text-transform: uppercase;
    margin-bottom: var(--space-s);
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
  }

  .main-blog-collection__post-title {
    margin-bottom: var(--space-2xs);
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
  }

  .main-blog-collection__post-title a,
  .main-blog-collection__post-link-container {
    color: var(--neutral-950);
    text-decoration: none;
  }

  .main-blog-collection__post-link {
    display: flex;
    gap: var(--space-xs);
  }

  .main-blog-collection__pagination {
    display: flex;
    justify-content: center;
    margin-top: var(--space-xl);
    padding: var(--space-m);
  }

  .main-blog-collection__pagination ul {
    display: flex;
    list-style: none;
    gap: var(--space-xs);
    align-items: center;
  }

  .main-blog-collection__pagination li {
    display: flex;
  }

  .main-blog-collection__pagination a,
  .main-blog-collection__pagination span {
    padding: var(--space-xs) var(--space-s);
    color: var(--neutral-600);
    text-decoration: none;
    border-radius: 0;
  }

  .main-blog-collection__pagination a:hover,
  .main-blog-collection__pagination .current {
    color: var(--neutral-950);
    background-color: var(--neutral-50);
  }

  @media screen and (max-width: 1440px) {
    .main-blog-collection {
      padding-bottom: var(--space-xl);
    }

    .main-blog-collection__grid {
      grid-template-columns: repeat(3, 1fr);
      gap: calc(var(--space-xs) + var(--space-xs));
      column-gap: var(--space-2xs);
    }

    .main-blog-collection__post-content {
      padding-right: var(--space-m);
    }
  }

  @media screen and (max-width: 570px) {
    .main-blog-collection__grid {
      grid-template-columns: 1fr;
      gap: calc(var(--space-xs) + var(--space-xs));
    }
  }
</style>

{% paginate blog.articles by section.settings.limit %}
  <div class="main-blog-collection">
    <div class="main-blog-collection__grid">
      {% for article in blog.articles %}
        <article class="main-blog-collection__post">
          {% if article.image %}
            <a href="{{ article.url }}" class="main-blog-collection__post-image">
              {{
                article.image
                | image_url: width: 850
                | image_tag:
                  loading: 'lazy',
                  decoding: 'async',
                  alt: article.title,
                  widths: '400, 600, 800, 1000, 1200, 1400, 1600, 1800, 2000, 2400, 3000',
                  sizes: '(max-width: 400px) 400px, (max-width: 600px) 600px, (max-width: 800px) 800px, (max-width: 1000px) 1000px, (max-width: 1200px) 1200px, (max-width: 1400px) 1400px, (max-width: 1600px) 1600px, (max-width: 1800px) 1800px, (max-width: 2000px) 2000px, (max-width: 2400px) 2400px, 100vw'
              }}
            </a>
          {% endif %}

          <div class="main-blog-collection__post-content">
            {% if article.tags.size > 0 %}
              <div class="main-blog-collection__post-category small">{{ article.tags.first }}</div>
            {% endif %}

            <h3 class="main-blog-collection__post-title heading--l">
              <a href="{{ article.url }}">{{ article.title }}</a>
            </h3>

            <a href="{{ article.url }}" class="main-blog-collection__post-link-container">
              <span class="main-blog-collection__post-link body">Read The Article {% render 'icon-arrow-submit-dark' %}</span>
            </a>
          </div>
        </article>
      {% endfor %}
    </div>

    {% if paginate.pages > 1 %}
      <div class="main-blog-collection__pagination">
        {% render 'pagination', paginate: paginate %}
      </div>
    {% endif %}
  </div>
{% endpaginate %}

{% schema %}
{
  "name": "Blog Collection",
  "limit": 1,
  "tag": "section",
  "class": "blog-products-section",
  "enabled_on": {
    "templates": ["page", "blog"]
  },
  "settings": [
    {
      "type": "range",
      "id": "limit",
      "label": "Limit",
      "info": "Limit the number of articles displayed per page.",
      "min": 4,
      "max": 50,
      "step": 1,
      "default": 8
    },
    {
      "type": "range",
      "id": "articles_per_row",
      "label": "Articles Per Row",
      "info": "Number of articles per row.",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3
    }
  ]
}
{% endschema %}