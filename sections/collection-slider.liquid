{% comment %}
  Collection Slider
  - Displays heading, description and link to collection on left
  - Displays a Splide slider with products from selected collection on right
  - Responsive: stacks on mobile, 1 column slider on mobile vs 3 on desktop
{% endcomment %}

<section class="collection-slider" id="section-{{ section.id }}">
  <div class="grid-2 collection-slider__container">
    <div class="collection-slider__content">
      {% if section.settings.heading != blank %}
        <h2 class="heading--2xl collection-slider__heading">{{ section.settings.heading }}</h2>
      {% endif %}

      {% if section.settings.description != blank %}
        <p class="body collection-slider__description">{{ section.settings.description }}</p>
      {% endif %}

      {% if section.settings.collection != blank and section.settings.button_label != blank %}
        <a href="{{ section.settings.collection.url }}" class="body--uppercase collection-slider__link">
          {{ section.settings.button_label }}
          {% render 'icon-arrow-submit' %}
        </a>
      {% endif %}
    </div>

    <!-- Right side: Product slider -->
    {% if section.settings.collection != blank %}
      <div class="collection-slider__slider-wrapper">
        <div class="collection-slider__slider splide">
          <div class="splide__track">
            <ul class="splide__list">
              {% assign product_limit = section.settings.products_to_show | default: 10 %}
              {% for product in section.settings.collection.products limit: product_limit %}
                <li class="splide__slide collection-slider__slide">
                  <a href="{{ product.url }}" class="collection-slider__product-link">
                    <div class="collection-slider__product-image-wrapper">
                      {% if product.featured_image != blank %}
                        <img
                          src="{{ product.featured_image | image_url: width: '500x' }}"
                          alt="{{ product.featured_image.alt | escape }}"
                          loading="lazy"
                          width="{{ product.featured_image.width }}"
                          height="{{ product.featured_image.height }}"
                          class="collection-slider__product-image"
                        >
                      {% else %}
                        {{ 'product-1' | placeholder_svg_tag: 'collection-slider__product-image placeholder' }}
                      {% endif %}
                    </div>

                    <h3 class="heading--l collection-slider__product-title">{{ product.title }}</h3>

                    {% if product.metafields.custom.material != blank %}
                      <p class="body collection-slider__product-material">{{ product.metafields.custom.material }}</p>
                    {% endif %}

                    <p class="body--bold collection-slider__product-price">
                      {% if product.compare_at_price > product.price %}
                        <span class="collection-slider__product-price--sale">{{ product.price | money }}</span>
                        <span class="collection-slider__product-price--compare">
                          {{- product.compare_at_price | money -}}
                        </span>
                      {% else %}
                        {{ product.price | money }}
                      {% endif %}
                    </p>
                  </a>
                </li>
              {% endfor %}
            </ul>
          </div>
        </div>
      </div>
    {% endif %}
  </div>
</section>

<style>
  .collection-slider {
    padding: var(--space-xl) var(--space-m);
  }

  .collection-slider__container {
    gap: var(--space-l);
  }

  .collection-slider__content {
    display: flex;
    flex-direction: column;
    gap: var(--space-m);
  }

  .collection-slider__description {
    max-width: 90%;
  }

  .collection-slider__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    text-decoration: none;
    color: var(--neutral-950);
    margin-top: var(--space-xs);
  }

  .collection-slider__slider-wrapper {
    overflow: visible;
  }

  .collection-slider__slider {
    margin-right: calc(-1 * var(--space-m));
  }

  .collection-slider__product-link {
    display: block;
    text-decoration: none;
    color: var(--neutral-950);
  }

  .collection-slider__product-image-wrapper {
    margin-bottom: var(--space-xs);
    aspect-ratio: 1 / 1.25;
    overflow: hidden;
  }

  .collection-slider__product-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .collection-slider__product-title {
    margin: var(--space-xs) 0 var(--space-2xs);
  }

  .collection-slider__product-material {
    margin-bottom: var(--space-2xs);
    color: var(--neutral-600);
  }

  .collection-slider__product-price--compare {
    text-decoration: line-through;
    color: var(--neutral-500);
    margin-left: var(--space-2xs);
  }

  @media screen and (max-width: 768px) {
    .collection-slider__container {
      grid-template-columns: 1fr;
    }

    .collection-slider__content {
      margin-bottom: var(--space-l);
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const splideElement = document.querySelector('#section-{{ section.id }} .collection-slider__slider');

    if (splideElement) {
      new Splide(splideElement, {
        perPage: 3,
        gap: 'var(--space-m)',
        arrows: true,
        pagination: false,
        drag: true,
        snap: true,
        breakpoints: {
          768: {
            perPage: 1,
          },
          1024: {
            perPage: 2,
          },
        },
      }).mount();
    }
  });
</script>

{% schema %}
{
  "name": "Collection Slider",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "New In Now"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Layer up with premium wool/cashmere blends."
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Shop Now"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 3,
      "max": 12,
      "step": 1,
      "default": 10,
      "label": "Maximum products to show"
    }
  ],
  "presets": [
    {
      "name": "Collection Slider",
      "category": "Collection"
    }
  ]
}
{% endschema %}
