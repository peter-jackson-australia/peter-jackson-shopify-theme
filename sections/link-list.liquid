{% comment %}
  Link List Section
{% endcomment %}

{{ 'section-link-list.css' | asset_url | stylesheet_tag }}

<div
  class="link-list link-list--align-{{ section.settings.alignment }}"
  style="
    {% if section.settings.show_top_border %}border-top: 1px solid var(--neutral-50);{% endif %}
    {% if section.settings.show_bottom_border %}border-bottom: 1px solid var(--neutral-50);{% endif %}
  "
>
  <div class="link-list__wrapper">
    {% if section.settings.heading != blank %}
      <div class="link-list__title">
        <h2 class="heading--xl">{{ section.settings.heading }}</h2>
      </div>
    {% endif %}

    <div class="link-list__content">
      <ul class="link-list__list">
        {% for block in section.blocks %}
          {% case block.type %}
            {% when 'link' %}
              {% if block.settings.link_text != blank and block.settings.link_url != blank %}
                <li>
                  <a href="{{ block.settings.link_url }}" class="link-list__link body--uppercase">
                    {{ block.settings.link_text }}
                  </a>
                </li>
              {% endif %}
          {% endcase %}
        {% endfor %}
      </ul>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Link List",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "info": "Optional heading for the link list"
    },
    {
      "type": "select",
      "id": "alignment",
      "label": "Desktop Alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "checkbox",
      "id": "show_top_border",
      "label": "Show top border",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_bottom_border",
      "label": "Show bottom border",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "link",
      "name": "Link",
      "settings": [
        {
          "type": "text",
          "id": "link_text",
          "label": "Link Text"
        },
        {
          "type": "url",
          "id": "link_url",
          "label": "Link URL"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Link List",
      "blocks": [
        {
          "type": "link",
          "settings": {
            "link_text": "Shop All",
            "link_url": "/collections/all"
          }
        },
        {
          "type": "link",
          "settings": {
            "link_text": "New Arrivals",
            "link_url": "/collections/new-arrivals"
          }
        },
        {
          "type": "link",
          "settings": {
            "link_text": "Best Sellers",
            "link_url": "/collections/best-sellers"
          }
        }
      ]
    }
  ]
}
{% endschema %}
