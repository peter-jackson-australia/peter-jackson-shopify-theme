{% comment %}
  Shop the Look section - Debug Output
{% endcomment %}

{% comment %} This part succesfully loops through the Shop the look metafield and pulls in the neccessary information {% endcomment %}
<div class="debug-output" style="padding: 20px; background: #f5f5f5; margin-bottom: 20px; border: 1px solid #ddd;">
  <h3>Debug: Shop the Look Products</h3>

  {% for item in product.metafields.custom.shop_the_look.value %}
    <div style="margin-bottom: 15px; padding: 10px; border: 1px solid #ccc;">
      <ul style="list-style: none; padding: 0;">
        <li><strong>Title:</strong> {{ item.title }}</li>
        <li><strong>Price:</strong> {{ item.price | money }}</li>
        <li><strong>Compare At Price:</strong> {{ item.compare_at_price | money }}</li>

        <li>
          <strong>Link:</strong> <a href="/products/{{ item.handle }}" target="_blank">/products/{{ item.handle }}</a>
        </li>

        <li><strong>Featured Image:</strong> {{ item.featured_image | image_url: width: 450 }}</li>
      </ul>
    </div>
  {% endfor %}

  {% if product.metafields.custom.shop_the_look == blank or product.metafields.custom.shop_the_look.value.size == 0 %}
    <h4>No shop the look products found</h4>
  {% endif %}
</div>

{% comment %} Check if we have a metafield and multiple images {% endcomment %}
{% if product.metafields.custom.shop_the_look != blank and product.images.size > 1 %}
  {% comment %} Get the second image {% endcomment %}
  {% assign second_image = product.images[1] %}

  <style>
    .shop-the-look {
      display: flex;
      overflow: hidden;
    }

    .shop-the-look__slider-container {
      width: 50vw;
      padding: var(--space-2xl) var(--space-m);
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .shop-the-look__heading {
      margin-bottom: var(--space-l);
      color: var(--neutral-950);
      text-align: center;
    }

    .shop-the-look__slider-wrapper {
      overflow: visible;
      width: 100%;
    }

    .shop-the-look__slider {
      width: calc(100% + var(--space-m));
      margin: 0 auto;
      position: relative;
    }

    .shop-the-look__image-container {
      width: 50vw;
      position: relative;
      overflow: hidden;
      background-color: var(--neutral-50);
    }

    .shop-the-look__image {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
      object-position: center;
    }

    .shop-the-look__product-link {
      display: block;
      text-decoration: none;
      color: var(--neutral-950);
    }

    .shop-the-look__product-image-wrapper {
      margin-bottom: var(--space-xs);
      overflow: hidden;
      background-color: var(--neutral-50);
      position: relative;
    }

    .shop-the-look__product-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .shop-the-look__product-title {
      margin: var(--space-xs) 0 var(--space-2xs);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      color: var(--neutral-950);
      text-align: center;
    }

    .shop-the-look__product-price {
      color: var(--neutral-950);
      text-align: center;
    }

    .shop-the-look__product-price--compare {
      text-decoration: line-through;
      color: var(--neutral-600);
      margin-left: var(--space-2xs);
    }

    .shop-the-look__single-product {
      padding: var(--space-m);
      max-width: 400px;
    }

    /* Arrow styling */
    .shop-the-look .splide__arrows {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 10;
    }

    .shop-the-look .splide__arrow {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      width: 30px;
      height: 30px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      pointer-events: auto;
      opacity: 0.9;
      border: none;
    }

    .shop-the-look .splide__arrow--prev {
      left: -24px;
    }

    .shop-the-look .splide__arrow--next {
      right: -24px;
    }

    @media screen and (max-width: 768px) {
      .shop-the-look {
        flex-direction: column;
      }

      .shop-the-look__slider-container {
        width: 100%;
        padding-top: var(--space-xl);
        padding-bottom: var(--space-xl);
      }

      .shop-the-look__image-container {
        display: none;
      }

      .shop-the-look__slider {
        width: calc(100% + var(--space-l));
      }

      .shop-the-look__product-title {
        width: calc(100% - var(--space-m));
      }
    }

    @media screen and (max-width: 470px) {
      .shop-the-look__slider-wrapper {
        overflow: hidden;
      }
    }
  </style>

  <div class="shop-the-look" id="section-{{ section.id }}">
    <div class="shop-the-look__slider-container">
      <h2 class="heading--xl shop-the-look__heading">{{ section.settings.heading }}</h2>

      {% comment %} Use a multi-product slider only if we have more than one product {% endcomment %}
      {% if all_products['albinoni-black-penny-loafer'] and all_products['alessio-brown-pebbled-leather-bag'] %}
        <div class="shop-the-look__slider-wrapper">
          <div class="shop-the-look__slider splide">
            <div class="splide__track">
              <ul class="splide__list">
                {% if all_products['albinoni-black-penny-loafer'] %}
                  {% assign product_item = all_products['albinoni-black-penny-loafer'] %}
                  <li class="splide__slide shop-the-look__slide">
                    <a href="{{ product_item.url }}" class="shop-the-look__product-link">
                      <div class="shop-the-look__product-image-wrapper">
                        {% if product_item.featured_image != blank %}
                          <img
                            src="{{ product_item.featured_image | image_url: width: '600px' }}"
                            alt="{{ product_item.featured_image.alt | escape }}"
                            loading="lazy"
                            width="{{ product_item.featured_image.width }}"
                            height="{{ product_item.featured_image.height }}"
                            class="shop-the-look__product-image"
                          >
                        {% else %}
                          {{ 'product-1' | placeholder_svg_tag: 'shop-the-look__product-image placeholder' }}
                        {% endif %}
                      </div>

                      <h3 class="body--bold shop-the-look__product-title">{{ product_item.title }}</h3>

                      <p class="small shop-the-look__product-price">
                        {% if product_item.compare_at_price > product_item.price %}
                          <span class="shop-the-look__product-price--sale">{{ product_item.price | money }}</span>
                          <span class="shop-the-look__product-price--compare">
                            {{- product_item.compare_at_price | money -}}
                          </span>
                        {% else %}
                          {{ product_item.price | money }}
                        {% endif %}
                      </p>
                    </a>
                  </li>
                {% endif %}

                {% if all_products['alessio-brown-pebbled-leather-bag'] %}
                  {% assign product_item = all_products['alessio-brown-pebbled-leather-bag'] %}
                  <li class="splide__slide shop-the-look__slide">
                    <a href="{{ product_item.url }}" class="shop-the-look__product-link">
                      <div class="shop-the-look__product-image-wrapper">
                        {% if product_item.featured_image != blank %}
                          <img
                            src="{{ product_item.featured_image | image_url: width: '600px' }}"
                            alt="{{ product_item.featured_image.alt | escape }}"
                            loading="lazy"
                            width="{{ product_item.featured_image.width }}"
                            height="{{ product_item.featured_image.height }}"
                            class="shop-the-look__product-image"
                          >
                        {% else %}
                          {{ 'product-1' | placeholder_svg_tag: 'shop-the-look__product-image placeholder' }}
                        {% endif %}
                      </div>

                      <h3 class="body--bold shop-the-look__product-title">{{ product_item.title }}</h3>

                      <p class="small shop-the-look__product-price">
                        {% if product_item.compare_at_price > product_item.price %}
                          <span class="shop-the-look__product-price--sale">{{ product_item.price | money }}</span>
                          <span class="shop-the-look__product-price--compare">
                            {{- product_item.compare_at_price | money -}}
                          </span>
                        {% else %}
                          {{ product_item.price | money }}
                        {% endif %}
                      </p>
                    </a>
                  </li>
                {% endif %}

                {% if all_products['aqua-sunset-pocket-square'] %}
                  {% assign product_item = all_products['aqua-sunset-pocket-square'] %}
                  <li class="splide__slide shop-the-look__slide">
                    <a href="{{ product_item.url }}" class="shop-the-look__product-link">
                      <div class="shop-the-look__product-image-wrapper">
                        {% if product_item.featured_image != blank %}
                          <img
                            src="{{ product_item.featured_image | image_url: width: '600px' }}"
                            alt="{{ product_item.featured_image.alt | escape }}"
                            loading="lazy"
                            width="{{ product_item.featured_image.width }}"
                            height="{{ product_item.featured_image.height }}"
                            class="shop-the-look__product-image"
                          >
                        {% else %}
                          {{ 'product-1' | placeholder_svg_tag: 'shop-the-look__product-image placeholder' }}
                        {% endif %}
                      </div>

                      <h3 class="body--bold shop-the-look__product-title">{{ product_item.title }}</h3>

                      <p class="small shop-the-look__product-price">
                        {% if product_item.compare_at_price > product_item.price %}
                          <span class="shop-the-look__product-price--sale">{{ product_item.price | money }}</span>
                          <span class="shop-the-look__product-price--compare">
                            {{- product_item.compare_at_price | money -}}
                          </span>
                        {% else %}
                          {{ product_item.price | money }}
                        {% endif %}
                      </p>
                    </a>
                  </li>
                {% endif %}
              </ul>
            </div>
            <div class="splide__arrows">
              <button class="splide__arrow splide__arrow--prev">{% render 'icon-arrow-see-less' %}</button>
              <button class="splide__arrow splide__arrow--next">{% render 'icon-arrow-see-more' %}</button>
            </div>
          </div>
        </div>
      {% else %}
        <div class="shop-the-look__single-product">
          {% if all_products['albinoni-black-penny-loafer'] %}
            {% assign product_item = all_products['albinoni-black-penny-loafer'] %}
          {% elsif all_products['alessio-brown-pebbled-leather-bag'] %}
            {% assign product_item = all_products['alessio-brown-pebbled-leather-bag'] %}
          {% elsif all_products['aqua-sunset-pocket-square'] %}
            {% assign product_item = all_products['aqua-sunset-pocket-square'] %}
          {% endif %}

          <a href="{{ product_item.url }}" class="shop-the-look__product-link">
            <div class="shop-the-look__product-image-wrapper">
              {% if product_item.featured_image != blank %}
                <img
                  src="{{ product_item.featured_image | image_url: width: '600px' }}"
                  alt="{{ product_item.featured_image.alt | escape }}"
                  loading="lazy"
                  width="{{ product_item.featured_image.width }}"
                  height="{{ product_item.featured_image.height }}"
                  class="shop-the-look__product-image"
                >
              {% else %}
                {{ 'product-1' | placeholder_svg_tag: 'shop-the-look__product-image placeholder' }}
              {% endif %}
            </div>

            <h3 class="body--bold shop-the-look__product-title">{{ product_item.title }}</h3>

            <p class="small shop-the-look__product-price">
              {% if product_item.compare_at_price > product_item.price %}
                <span class="shop-the-look__product-price--sale">{{ product_item.price | money }}</span>
                <span class="shop-the-look__product-price--compare">
                  {{- product_item.compare_at_price | money -}}
                </span>
              {% else %}
                {{ product_item.price | money }}
              {% endif %}
            </p>
          </a>
        </div>
      {% endif %}
    </div>

    <div class="shop-the-look__image-container">
      {% assign image_width = 1024 %}
      {% assign image_height = image_width | divided_by: second_image.aspect_ratio | ceil %}
      <img
        src="{{ second_image.src | image_url: width: image_width }}"
        srcset="
          {{ second_image.src | image_url: width: 600 }} 600w,
          {{ second_image.src | image_url: width: 800 }} 800w,
          {{ second_image.src | image_url: width: 1024 }} 1024w,
          {{ second_image.src | image_url: width: 1440 }} 1440w
        "
        sizes="(max-width: 768px) 100vw, 50vw"
        width="{{ image_width }}"
        height="{{ image_height }}"
        alt="{{ second_image.alt | default: product.title }}"
        loading="lazy"
        class="shop-the-look__image"
      >
    </div>
  </div>

  {% comment %} Initialize the slider if we have multiple products {% endcomment %}
  {% if all_products['albinoni-black-penny-loafer'] and all_products['alessio-brown-pebbled-leather-bag'] %}
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        const splideElement = document.querySelector('#section-{{ section.id }} .shop-the-look__slider');

        if (splideElement) {
          new Splide(splideElement, {
            perPage: 1,
            width: '500px',
            type: 'loop',
            gap: 'var(--space-m)',
            arrows: true,
            pagination: false,
            drag: true,
            snap: true,
            breakpoints: {
              470: {
                perPage: 1,
              },
              768: {
                arrows: true,
              },
            },
          }).mount();
        }
      });
    </script>
  {% endif %}
{% endif %}

{% schema %}
{
  "name": "Shop the Look",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Shop the Look"
    }
  ],
  "presets": [
    {
      "name": "Shop the Look",
      "category": "Product"
    }
  ]
}
{% endschema %}
